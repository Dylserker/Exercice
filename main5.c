#include <stdlib.h>
#include <stdio.h>
int main()
{ 
    double number_1 = 12; 
    double number_2 = 7;
    double result = (number_1 + number_2) / 2;
    
    printf("La moyenne de 12 et 7 est égale à %.2f\n", result);
    exit(0);
}
